<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Priority Indicator -->
        <View
            android:id="@+id/vPriorityIndicator"
            android:layout_width="4dp"
            android:layout_height="0dp"
            android:layout_marginEnd="12dp"
            android:background="@color/priority_high"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Header Row -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/on_surface"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/tvId"
            app:layout_constraintStart_toEndOf="@id/vPriorityIndicator"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Problema con maquinaria de soldadura" />

        <TextView
            android:id="@+id/tvId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_tarjeta_id"
            android:paddingHorizontal="8dp"
            android:paddingVertical="4dp"
            android:textColor="@color/primary"
            android:textSize="12sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="#001" />

        <!-- Description -->
        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/on_surface_variant"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/vPriorityIndicator"
            app:layout_constraintTop_toBottomOf="@id/tvTitle"
            tools:text="La máquina de soldadura presenta fallas intermitentes que afectan la productividad del equipo" />

        <!-- Category and Status Row -->
        <LinearLayout
            android:id="@+id/llChips"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="horizontal"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/vPriorityIndicator"
            app:layout_constraintTop_toBottomOf="@id/tvDescription">

            <com.google.android.material.chip.Chip
                android:id="@+id/chipCategory"
                style="@style/Widget.Material3.Chip.Assist"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                app:chipBackgroundColor="@color/surface_variant"
                app:chipIconTint="@color/on_surface_variant"
                app:chipMinHeight="28dp"
                tools:chipIcon="@drawable/ic_category"
                tools:text="Maquinaria" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chipStatus"
                style="@style/Widget.Material3.Chip.Assist"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:chipMinHeight="28dp"
                tools:text="Abierta" />

        </LinearLayout>

        <!-- Bottom Info Row -->
        <TextView
            android:id="@+id/tvAuthor"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:layout_marginEnd="8dp"
            android:drawablePadding="4dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/on_surface_variant"
            android:textSize="12sp"
            app:drawableStartCompat="@drawable/ic_person_small"
            app:drawableTint="@color/on_surface_variant"
            app:layout_constraintEnd_toStartOf="@id/tvDate"
            app:layout_constraintStart_toEndOf="@id/vPriorityIndicator"
            app:layout_constraintTop_toBottomOf="@id/llChips"
            tools:text="Juan Pérez" />

        <TextView
            android:id="@+id/tvDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:drawablePadding="4dp"
            android:textColor="@color/on_surface_variant"
            android:textSize="12sp"
            app:drawableStartCompat="@drawable/ic_calendar"
            app:drawableTint="@color/on_surface_variant"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tvAuthor"
            tools:text="15/08/2025" />

        <!-- Priority Badge (conditional) -->
        <ImageView
            android:id="@+id/ivPriorityBadge"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginTop="8dp"
            android:contentDescription="Alta prioridad"
            android:src="@drawable/ic_priority_high"
            android:visibility="gone"
            app:layout_constraintStart_toEndOf="@id/vPriorityIndicator"
            app:layout_constraintTop_toBottomOf="@id/tvAuthor"
            app:tint="@color/priority_critical"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/tvPriorityText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="4dp"
            android:text="CRÍTICA"
            android:textColor="@color/priority_critical"
            android:textSize="11sp"
            android:textStyle="bold"
            android:visibility="gone"
            app:layout_constraintBottom_toBottomOf="@id/ivPriorityBadge"
            app:layout_constraintStart_toEndOf="@id/ivPriorityBadge"
            app:layout_constraintTop_toTopOf="@id/ivPriorityBadge"
            tools:visibility="visible" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>